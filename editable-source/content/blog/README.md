# Blog files

The published and upcoming field notes live here. To publish a new note, copy a `Coming soon` file, set `status: Published`, write the `Post` paragraphs, and add it to the repository. The app automatically exposes published posts at `/notes/<id>`.
